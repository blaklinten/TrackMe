#!/bin/bash

clear
echo "What are you doing?"
read activity
START=$(date +%s)

echo "$activity" > $WD/active
echo "$1" >> $WD/active
echo "$START" >> $WD/active

echo "$activity@$1 started @$(date --date="@$START")"
