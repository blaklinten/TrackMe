#!/bin/bash

clear
echo "What are you doing?"
read activity
START=$(date +%s)

echo "$activity" > $ACTIVE
echo "$1" >> $ACTIVE
echo "$START" >> $ACTIVE

echo "$activity@$1 started @$(date --date="@$START")"
