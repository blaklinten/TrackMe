#!/bin/bash

# These are the configuration options
# for the TrackMe script
#
# Use git sync yes/no
export GITSYNC=false
###### TODO ######
# Fix info if    #
# git is not     #
# available      #
##################

# Export enviroment variables
export PWD=/home/blaklinten/Git/TrackMe # This is where your program lives
export WD=$PWD
export TRACKME=$HOME/TrackMe
export PROJECTS=$TRACKME/Projects
export ACTIVE=$TRACKME/active

# Check if project destination is ok
# if not, fix it
if [[ ! -d $PROJECTS ]]; then
    mkdir -p $PROJECTS
    echo "Creating working env..."
fi
