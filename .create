#!/bin/bash

createClient()
{
	CLIENT=$(parseSpaces $(showAndRead "Enter name of new client:"))

	mkdir "$CLIENTS/$CLIENT"

	chooseClient "$SCRIPT"
}

createProject()
{
	projectName=$(parseSpaces $(showAndRead "Enter name of new project:"))

	mkdir "$PROJECTS/$projectName"

	chooseProject "$SCRIPT"
}
